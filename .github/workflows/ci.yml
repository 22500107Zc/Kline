name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # The browser half of the suite needs a real Chromium. Without one it
      # used to skip itself and the job went green having run none of the
      # tests that look at the screen.
      - name: Install the browser the interaction tests need
        run: npx playwright install --with-deps chromium
      - name: Typecheck
        run: npm run typecheck
      - name: Unit and browser tests
        run: npm test
      - name: Production build
        run: npx vite build

  desktop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx vite build
      - name: Smoke-test the desktop shell
        # Boots the real Electron app under a virtual display, screenshots it
        # and exits — proves the packaged shell renders, not just the web build.
        run: xvfb-run -a npx electron . --no-sandbox --enable-unsafe-swiftshader
        env:
          KILN_SMOKE: ${{ runner.temp }}/kiln-desktop.png
      - uses: actions/upload-artifact@v4
        with:
          name: desktop-smoke-screenshot
          path: ${{ runner.temp }}/kiln-desktop.png
